# Physical TSC TE244 validation

Run only after Phase 3.1 CI and review. Print Amazon short/long title, long SKU/address/email, 1 N, 2 N, odd/even quantities, then scan FNSKU. Print two examples of all seven Flipkart formats plus a worst-case pendant; verify content, readability, barcode scans, protected zones, clipping, 2-up pairing, and quantity.

`spooled` means Windows accepted RAW bytes, not guaranteed paper output. Simulation cannot approve. Approve only exact tested printer/profile/renderer/version/layout and Flipkart format after a passing server-derived scan.

