import { FormEvent,useState } from "react";
import { api } from "../../api/client";
import type { CurrentUser } from "../../types";

export function LoginPage({onLogin}:{onLogin:(user:CurrentUser)=>void}){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");async function submit(event:FormEvent){event.preventDefault();setError("");try{onLogin(await api<CurrentUser>("/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})}))}catch(exc:any){setError(exc.message)}}return <main className="login-page"><form onSubmit={e=>void submit(e)}><span className="eyebrow">MMS LOCAL OFFICE</span><h1>Sign in</h1><p>Use your assigned account. Printer and quality actions are role-protected.</p>{error&&<p className="inline-error">{error}</p>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label><button className="primary" type="submit">Sign in</button></form></main>}
